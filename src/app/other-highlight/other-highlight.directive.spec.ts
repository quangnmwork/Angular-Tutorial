import { OtherHighlightDirective } from './other-highlight.directive';

describe('OtherHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OtherHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
